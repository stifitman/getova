<%= form_for(@complu) do |f| %>
  <% if @complu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@complu.errors.count, "error") %> prohibited this complu from being saved:</h2>

      <ul>
      <% @complu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
