<%= form_for(@tanet) do |f| %>
  <% if @tanet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tanet.errors.count, "error") %> prohibited this tanet from being saved:</h2>

      <ul>
      <% @tanet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
