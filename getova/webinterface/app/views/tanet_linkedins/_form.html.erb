<%= form_for(@tanet_linkedin) do |f| %>
  <% if @tanet_linkedin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tanet_linkedin.errors.count, "error") %> prohibited this tanet_linkedin from being saved:</h2>

      <ul>
      <% @tanet_linkedin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
